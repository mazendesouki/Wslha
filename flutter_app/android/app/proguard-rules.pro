# Flutter's Gradle plugin references this file for release (R8) builds.
# No custom rules needed yet — Flutter's own consumer ProGuard rules
# (bundled with the engine) already cover the framework itself.
