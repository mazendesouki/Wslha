// Firebase temporarily stubbed out for a diagnostic build (see
// pubspec.yaml — firebase_core/firebase_messaging commented out). Do not
// leave this stubbed in a real release; restore the Firebase imports and
// PushRegistrar body below once the diagnostic test is done.
import 'session.dart';

class PushRegistrar {
  PushRegistrar._();
  static Future<void> registerForSession(UserSession session) async {}
}
