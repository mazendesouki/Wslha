-- =====================================================================
--  وصّلها — رجوع "الماركة والموديل" في تسجيل السائق لكتابة حرة
--
--  كنا ضفنا قيد foreign key يربط driver_applications.vehicle_model بكتالوج
--  vehicle_rates (عشان يمنع تسجيل موديل غير مسعّر). دلوقتي السائق هيكتب
--  ماركة/موديل عربيته حر — فلازم نشيل القيد ده، وإلا أي طلب هيترفض.
--
--  ملاحظة: فئة السيارة (vehicle_category: سيدان/SUV/فان) هي اللي بتحدد
--  شريحة التسعير — مش الموديل النصي — فالتسعير مايتأثرش بالتغيير ده.
--
--  شغّله مرة واحدة في SQL Editor. آمن لإعادة التشغيل.
-- =====================================================================
set search_path = public, extensions;

alter table public.driver_applications
  drop constraint if exists driver_applications_vehicle_model_fkey;
