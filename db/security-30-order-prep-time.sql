-- =====================================================================
--  وصّلها — عمود وقت التجهيز (لعداد التنازلي في لوحة استقبال طلبات التاجر)
--
--  مفيش عمود ETA/prep-time للطلب أصلاً — التاجر لما يوافق على الطلب بيختار
--  كام دقيقة محتاج للتجهيز، والتطبيق بيعرض عداد تنازلي حي على كارت الطلب
--  بيتحول لأحمر/عاجل لو اتأخر عن الوقت ده.
--
--  آمن لإعادة التشغيل. شغّله كامل مرة واحدة في SQL Editor.
-- =====================================================================
set search_path = public, extensions;

alter table public.orders add column if not exists prep_minutes integer;
